import styled from 'styled-components';

export const FormInput = styled.input`
  margin: 1rem 0;
  padding: 0.5rem;
  font-size: 16px;
  border: 1px solid #ccc;
  border-radius: 4px;
  outline: none;

  &:focus {
    border-color: #ffa07a;
  }
`;
